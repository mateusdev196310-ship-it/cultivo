services:
  - type: web
    name: cultiva-app
    runtime: static
    buildCommand: npm run build
    publishDir: dist
    envVars:
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false
